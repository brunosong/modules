kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: argocd-migration
nodes:
  - role: control-plane
    extraPortMappings:
      # ArgoCD UI NodePort 접근용
      - containerPort: 30443
        hostPort: 30443
        protocol: TCP
