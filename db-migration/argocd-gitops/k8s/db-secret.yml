apiVersion: v1
kind: Secret
metadata:
  name: db-migration-secret
  namespace: default
type: Opaque
stringData:
  # ──────────────────────────────────────────
  # 로컬 테스트 (Kind + docker-compose MySQL)
  # ──────────────────────────────────────────
  DB_HOST: "host.docker.internal"
  DB_PORT: "3309"
  DB_NAME: "modules_db"
  DB_USERNAME: "migration"
  DB_PASSWORD: "migration1234"
  # ──────────────────────────────────────────
  # 운영 (NKS + NCP Cloud DB)
  # DB_HOST: "db-migration-mysql.cdb.ntruss.com"
  # DB_PORT: "3306"
  # DB_USERNAME: "migration"
  # DB_PASSWORD: "실제비밀번호"
  # ──────────────────────────────────────────
